[build]
  command = "npm run build"
  publish = "public"
  base = "/"

[context.production]
  command = "npm run build"
  publish = "public"

[context.branch-deploy]
  command = "npm run build"
  publish = "public"